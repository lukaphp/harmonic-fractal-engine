import type { RootContainer } from '@types';
import '../../index';
import type { CoreHostProps } from '@props';
export declare class Host {
    static createInstance(type: string, Class: any, props: CoreHostProps, rootContainer: RootContainer, cloneFn?: Function): any;
}
