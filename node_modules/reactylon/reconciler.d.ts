import { type FiberRoot } from 'react-reconciler';
import type { RootContainer, EngineContext } from '@types';
export declare const ROOT_IDENTIFIER: unique symbol;
type RenderCallbacks = {
    onFirstCommit?: () => void;
};
type ReactylonHandler = {
    render: (element: any, rootContainer: RootContainer, callbacks?: RenderCallbacks) => void;
    unmount: (rootContainer: RootContainer, disposeEngine: EngineContext['disposeEngine']) => void;
};
export declare const roots: Map<any, {
    root: FiberRoot;
    didFirstCommit: boolean;
}>;
declare const Reactylon: () => ReactylonHandler;
export default Reactylon;
